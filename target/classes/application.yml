server:
  port: 8083
  servlet:
    context-path: /api

spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3300/pfe_base
    username: root
    password: root@2022

  jpa:
    show_sql: true
    generate-ddl: true
    hibernate:
      ddl-auto: create